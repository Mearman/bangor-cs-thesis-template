\chapter{Implementation}
Describe how the system was built. Include the code that carries the
weight of the contribution, not everything you wrote.

\begin{lstlisting}[caption={A sample code listing.},label={lst:sample}]
def total(items):
    result = 0
    for item in items:
        result += item
    return result
\end{lstlisting}
