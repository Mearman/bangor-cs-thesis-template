\chapter{Implementation}
Describe how the system was built.

\section{Code listings}
\Cref{lst:sample} shows a code listing with syntax highlighting; adjust the language and style in \texttt{preamble-cs.tex}.

\begin{lstlisting}[caption={A sample code listing.},label={lst:sample}]
def total(items):
    result = 0
    for item in items:
        result += item
    return result
\end{lstlisting}
