\chapter{Evaluation}
State what you measured, how you measured it, and what you found. \Cref{tab:sample} shows the shape of a results table, and \cref{fig:crest} shows the university crest through the brand layer.

\begin{table}[htbp]
  \centering
  \caption{A sample results table.}
  \label{tab:sample}
  \begin{tabular}{lrr}
    \hline
    Method & Score & Time \\
    \hline
    Baseline & 0.85 & 1.2 \\
    Ours & 0.91 & 1.1 \\
    \hline
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \bangorcrest[4cm]
  \caption{The university crest, included through the brand layer.}
  \label{fig:crest}
\end{figure}

\section{Float numbering}
The class numbers floats by section by default. Figures, tables, and listings share one counter, and the counter restarts in each section, so \cref{tab:numbering} is the first float of this section and its number says so. Floats that come before the first section of a chapter, such as \cref{tab:sample}, are numbered by the chapter alone. Set \texttt{sharedfloats=false} or \texttt{sectionfloats=none} in the class options to turn either behaviour off.

\begin{table}[htbp]
  \centering
  \caption{Numbers under the float options.}
  \label{tab:numbering}
  \begin{tabular}{ll}
    \hline
    Option & Effect \\
    \hline
    \texttt{sharedfloats} & one sequence for every float type \\
    \texttt{sectionfloats} & the sequence restarts in each section \\
    \hline
  \end{tabular}
\end{table}

\section{Continuation pages}
This section spans multiple pages so you can see how a chapter looks
when it flows past its opening page. On the chapter's first page the
page style is plain: no header, page number centred at the bottom. On
every subsequent page the running header appears with the chapter name
on the left and the page number on the right, separated from the text
by a thin rule.

Replace this prose with your own content; it exists so the compiled
demonstration PDF shows continuation-page styling.

\subsection{First subsection}
The header text is set in italics at a smaller size than the body, so
it does not compete with the running text. The thin rule beneath it is
0.4 points wide, visible on screen and in print but unobtrusive. If
your school's conventions require a different header layout, the class
provides the fancyhdr infrastructure; override it in your preamble.

\subsection{Second subsection}
The page number sits in the header on continuation pages and in the
footer on chapter-opening pages. This dual placement is standard in UK
theses: the reader can find their place from either the top or bottom
of the page. The number is in the same typeface as the body text at
the same size, not the smaller italic used for the chapter name.

\subsection{Third subsection}
Each new section starts with its heading in the body text and does not
reset the running header; the header always shows the chapter name,
not the section name. This is a deliberate simplification: section
names change frequently within a chapter and the running header would
need to update mid-page, which fancyhdr handles only at page
boundaries and can produce inconsistent results when a section starts
near the bottom of a page.

\subsection{Fourth subsection}
The gap between the header and the body text is governed by the
headsep length, which the class sets to the report class default. If
you find the header too close or too far from your text, adjust
headsep in your preamble with a setlength command. The headheight has
already been set to 14 points to accommodate the italic chapter name
at 12 point body size without a fancyhdr warning.

\subsection{Fifth subsection}
This is the final subsection of the demonstration content. If you can
see the running header with the chapter name and page number above
this text, the continuation-page styling is working correctly. The
next chapter will open with the plain page style again, showing the
transition between the two modes.
